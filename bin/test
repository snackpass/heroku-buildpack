#!/usr/bin/env bash

BUILD_DIR=${1:-}
if yarn --version > /dev/null 2>&1; then
  cd "$BUILD_DIR" && yarn test
else
  ls $CACHE_DIR && cd "$BUILD_DIR" && npm test && echo $CACHE_DIR && ls $CACHE_DIR
fi
